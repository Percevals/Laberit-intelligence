/**
 * Admin Page
 * Main admin interface for managing companies and system data
 */

import { AdminCompanyManager } from '@/components/admin/AdminCompanyManager';

export function AdminPage() {
  return <AdminCompanyManager />;
}